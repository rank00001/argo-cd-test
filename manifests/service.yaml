apiVersion: v1
kind: Service
metadata:
  name: hello
spec:
  type: ClusterIP        # reach it via `kubectl port-forward` (no host-port clashes)
  selector:
    app: hello
  ports:
    - port: 5678
      targetPort: 5678
